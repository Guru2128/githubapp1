New File Added2.
New file.
18/8/26 changes.
add. //if we are use this all the page it will added. 

git add test2.txt
git commit -m "File Updated"
git push origin main

31-08-2026 ->read
01-09-2026 ->read
------------------------------------------------------------------------

2. Abstraction – Hiding implementation details and exposing only the required functionality. 
In C#, we achieve this using interfaces and abstract classes.

Interface → defines what to do
Class → defines how to do it
Hiding the implementation details → Abstraction
 
interface :
public interface IPayment
{
      // Validate card
        // Connect to payment gateway
        // Check balance
        // Process transaction
        // Save transaction
        
    void Pay();
}
public class CreditCardPayment : IPayment
{
    public void Pay()
    {
        Console.WriteLine("Payment done using Credit Card");
    }
}
--------------------------------------------------------------------------------------
Abstract Class Example
using System;
abstract class Animal
{
    // Abstract method
    public abstract void MakeSound();

    // Normal method
    public void Sleep()
    {
        Console.WriteLine("Animal is sleeping");
    }
}
class Dog : Animal
{
    // Implementing abstract method
    public override void MakeSound()
    {
        Console.WriteLine("Dog barks");
    }
}
class Program
{
    static void Main()
    {
        Animal dog = new Dog();  //you can't crete object in abstract class.

        dog.MakeSound();
        dog.Sleep();
    }
}
-------------------------------------------------------------